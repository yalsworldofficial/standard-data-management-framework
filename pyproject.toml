[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sdmf"
version = "0.1.0"
description = "SDMF - Standard Data Management Framework"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "Harsh Handoo", email = "yalsworldofficial@gmail.com" }
]

dependencies = [
  "pandas",
  "openpyxl"
]

[tool.setuptools.packages.find]
where = ["src"]